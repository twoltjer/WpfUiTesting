<Window x:Class="WpfTestApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTestApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
        <Button AutomationProperties.AutomationId="MyButton" Background="{Binding ButtonBackground}" Width="120" Height="50" Command="{Binding ButtonCommand}">Hello</Button>
    </Grid>
</Window>
